mixin header()
  +b.header&attributes(attributes)
    .container.header__container
      //+icon('logo').__logo
      +e.IMG(src='/assets/images/logo.svg').logo
      +b.UL.menu.header__menu
        +e.LI.item
          +link(href='#')._nounder._medium Компания
        +e.LI.item
          +link(href='#')._nounder._medium Услуги
        +e.LI.item
          +link(href='#')._nounder._medium Кейсы
        +e.LI.item
          +link(href='#')._nounder._medium Журнал
        +e.LI.item
          +link(href='#')._nounder._medium Клиенты
        +e.LI.item
          +link(href='#')._nounder._medium Вакансии
        +e.LI.item
          +link(href='#')._nounder._medium Контакты
      +b.buttons
        +button.buttons__ico ico
        +button._blue.buttons__project обсудить проект
        +e.burger.js-padmenu
          span

    block